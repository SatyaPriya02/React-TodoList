 
 import {createreducer} from "react-redux"

 const todo=createreducer({
    name:todo,
    initalState:{},
        reducers:{
          addtodo:(type,action)=>

          store.storeReducer
        }

 })